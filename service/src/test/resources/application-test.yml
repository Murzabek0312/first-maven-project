spring:
  datasource:
    username: test
    password: test
  jpa:
    properties:
      hibernate:
        show_sql: true
        hbm2ddl:
          auto: validate
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQL10Dialect